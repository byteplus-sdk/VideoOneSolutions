package com.vertc.api.example.utils;

import android.util.Log;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;

public class RTMPLinkGenerator {
    private static final String TAG = "RTMPLinkGenerator";

    @Nullable
    public static String create(@NonNull String taskId) {
        String url = null;
        return url;
    }
}
